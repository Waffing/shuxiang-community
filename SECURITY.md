# 安全政策

## 维护范围

当前安全修复面向 `main` 分支。使用旧快照部署的站点应自行评估更新；仓库包含权限、会话、下载与缓存边界测试，但这些测试不代表系统不存在漏洞。

## 私下报告问题

优先使用仓库 **Security → Report a vulnerability** 的私密报告入口（启用时可见）。如果入口尚未开放，请通过维护者 [GitHub 主页](https://github.com/Waffing) 的联系方式请求私下沟通；也可以在公开 Issue 仅请求一个私密联系渠道，不发布漏洞细节。

报告建议包含：

- 受影响的提交、组件与部署前提。
- 最小复现步骤、预期边界与实际结果。
- 使用自建隔离环境完成的证明。
- 潜在影响与可行的修复建议。

请勿附带生产密码、会话 Token、证书私钥、数据库转储或真实用户数据。不要对线上演示站点进行压力测试、批量探测或操作他人的数据。

维护者会根据复现结果评估影响、准备补丁并协商披露；目前没有固定响应时限或漏洞赏金承诺。

## 部署者需要保持的边界

- 使用独立随机密钥；`.env` 与数据库、Redis 不对公网公开。
- HTTPS、Cookie 安全标记与代理传递的认证 / 协议信息保持一致。
- 上传目录不执行 PHP；站内下载经权限、短时令牌与 Nginx 内部转发。
- 不共享缓存用户私有响应，不让 PWA 缓存 API、下载或 `no-store` 响应。
- 首次安装显式指定管理员，不依赖默认密码或隐藏后台地址。
- 保留已验证备份，并在隔离环境演练恢复。
- Android 正式发布使用自己的 Release 签名，不公开 Debug 包、签名密钥或测试凭证。

## 资源内容与信任状态

文件哈希证明的是内容一致性，不单独证明文件安全。扫描结果、来源检查与结构化验证记录也有各自的时间和覆盖范围。部署者需要独立审核资源来源、授权与投诉，不能把用户上传或代码开源许可视为第三方软件的分发授权。
